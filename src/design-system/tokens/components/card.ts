import { Gray, White } from '../color/primitives';
import { SpacingScale } from '../spacing/primitives';

export const CardTokens = {
	background: White,
	border: Gray[200],
	padding: SpacingScale.lg
};
